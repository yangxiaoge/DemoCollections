<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yang.bruce.newmiui">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--PermissionsActivity一定不可以忘记申明-->
        <activity android:name=".permission.PermissionsActivity" />
        <activity android:name=".radiobutton.CheckboxRadiobuttonDemoActivity" />
        <activity android:name=".webview_progress.MainWebViewActivity" />
        <activity android:name=".webview_progress.WebViewProgressActivity" />
        <activity android:name=".dynamic_image.DynamicActivity" />
        <activity android:name=".judge_json.JudgeJsonActivity" />
        <activity android:name=".group_listview.PinnedSectionListActivity" />
        <activity android:name=".bottombar.BottomBarActivity" />
        <activity android:name=".day_night_theme.activity.DayNightActivity" />
        <activity android:name=".day_night_theme.activity.AuthorActivity" />
        <activity android:name=".gridview.GridViewActivity" />
        <activity android:name=".reselect_radiobutton.ReSelectRadioButtonActivity" />
        <activity android:name=".refresh_layout.PublicRefreshLayout_Activity" />
        <activity android:name=".view_move.MoveViewDemoActivity" />
        <activity android:name=".MultiType_Use_Demo.MultiType_Demo_Activity" />
        <activity android:name=".nice_loading_progress.NiceLoadingProgressBar" />
        <activity android:name=".TranslucetActivity"
            android:theme="@style/Theme.Transparent"/>

        <!-- 静态注册 广播接收器 -->
        <!--<receiver android:name=".NetworkConnectChangedReceiver">
            <intent-filter>
                <action android:name="android.NET.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.Net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>-->
    </application>

</manifest>